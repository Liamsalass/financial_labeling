name: FINER-139

train:
  texts: finer_tokenized.npy  # Adjust to where the tokenized training data is saved
  labels: finer_labels.npy

valid:
  size: 1000  # Use 10% of the training data as validation data

test:
  texts: finer_tokenized.npy  # Adjust to where the tokenized testing data is saved

embedding:
  emb_init: emb_init.npy  # Adjust to where the embedding initialization is stored

output:
  res: results/finer_139  # Folder to save the results

labels_binarizer: results/mlb_finer.pkl  # Path for saving/loading the MultiLabel Binarizer
